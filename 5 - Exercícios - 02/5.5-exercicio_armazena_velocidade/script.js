let vel = 80;

if (vel <= 80) {
  console.log(
    `Sua velocidade foi ${vel} km/h, está dentro do limite permitido!`
  );
} else
  console.log(
    `Sua velocidade foi ${vel} km/h, está acima do limite permitido!`
  );
